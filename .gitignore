# Local working notes — not for the public repo
FUTURE.md

# OS
.DS_Store

# Node / Bun
node_modules/

# Electrobun build outputs
build/
artifacts/

# Generated pack artifacts (built by `bun run build:packs`)
src/packs/*/pack.wasm

# Headless render snapshots (produced by `src/packs/render.test.ts` and `scripts/render-pack.ts`)
tests/snapshots/

# Bind-mount target for `docker run -v $PWD/docker-out:/out cat-nip-headless ...`
docker-out/

# Gallery build output (produced by `bun run build:gallery`)
site/

# Signing certificates
*.p12

# Local agent state
.claude/settings.local.json
.opencode/
