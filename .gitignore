# Local working notes — not for the public repo
FUTURE.md

# OS
.DS_Store

# Node / Bun
node_modules/

# Electrobun build outputs
build/
artifacts/

# Generated pack artifacts (built by `bun run build:packs`)
src/packs/*/pack.wasm

# Headless render snapshots (produced by `src/packs/render.test.ts` and `scripts/render-pack.ts`)
tests/snapshots/

# Bind-mount target for `docker run -v $PWD/docker-out:/out music-visualizer-headless ...`
docker-out/

# Local agent state
.claude/settings.local.json
.opencode/
